<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.zois.ojt.dao.LoginDao">

	<select id="idcheck">
	
	select count(*) from member_table where userId = #{userId} and userPw = #{userPw};
	</select>
	
</mapper>